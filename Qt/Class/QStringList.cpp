/*      Description     */
문자열을 배열 형태로 관리할 수 있는 기능을 제공하는 클래스

/*      Header, qmake     */
#include <QStringList>
QT += core

/*      Constructor     */
QStringList(InputIterator first, InputIterator last);
QStringList(std::initializer_list<QString> args);
QStringList(QList<QString> &other);
QStringList(const QString &str);
QStringList();

/*      Method     */

// Overloading

// Arguments

// Example


/* =============================== */
